.DS_Store
__pycache__/
*.pyc

# Workspace created when the skill runs in this dir (don't ship transcripts/cache).
.youtube-research/

# Old workspace locations (pre-rename). Keep ignored so accidental old runs don't get committed.
.research-yt/
research-yt-kb/
